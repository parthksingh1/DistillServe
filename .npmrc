# pnpm's default isolated node_modules is what we want; this only makes the
# strictness explicit so a transitive dependency can never be imported by accident.
strict-peer-dependencies=false
auto-install-peers=true
